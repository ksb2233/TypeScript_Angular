/**
 * 리터럴 타입 좁히기 (Literal_Narrowing)
 * - var 또는 let 으로 변수를 선언할 경우, 해당 변수의 값이 변경될 가능성이 있음을 컴파일러에 알린다.
 * - const 로 변수를 선언하게되면 값이 변경되지 않음을 알린다.
 */
var helloWorld = "Hello World";
var hiWorld = "Hi World";
